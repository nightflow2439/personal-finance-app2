datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

enum RecordType {
  income
  expense
}

model Record {
  id Int @id @default(autoincrement())
  date DateTime @default(now())
  amount Float
  type RecordType
  note String
}
